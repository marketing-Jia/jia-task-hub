# Jia 協作需求中心｜Google Sheets 完整串接流程

這份文件只處理 Google Sheets 與 Apps Script 後端。完成本文件後，網站將可以：

- 將新任務寫入 Google Sheets。
- 依交付時間讀取及排序任務。
- 讓 Jia 更新待處理、進行中與已完成狀態。
- 儲存 Jia 的交付回覆。
- 讓發任務的人透過任務編號查詢進度，或用完整填寫人內容找回最近 10 筆任務。

Google 官方說明：

- [從 Google Sheets 建立綁定的 Apps Script](https://developers.google.com/apps-script/guides/bound)
- [Apps Script 網頁應用程式部署方式](https://developers.google.com/apps-script/guides/web)
- [Apps Script 授權機制](https://developers.google.com/apps-script/guides/services/authorization)

---

# 一、建立專用試算表

## 1. 建立檔案

1. 使用預計長期管理這個系統的公司 Google 帳號登入。
2. 建立一份空白 Google 試算表。
3. 將試算表命名為：`Jia 協作任務資料庫`。
4. 建議將檔案放在公司指定的 Google Drive 資料夾。

請避免使用個人臨時帳號。Apps Script 網頁應用程式會以部署者身分執行，若部署帳號離職、停權或將檔案移轉到不同網域，可能需要重新部署。

## 2. 不要先手動建立欄位

後端程式會自動建立名為「工作需求」的工作表與欄位。保留原本的空白工作表沒有關係。

系統使用的欄位順序如下：

| 欄 | 欄位 | 用途 |
| --- | --- | --- |
| A | 編號 | 每筆任務的追蹤編號 |
| B | 建立時間 | 發任務的時間 |
| C | 填寫人 | 發任務的人 |
| D | 項目類別 | 行銷、行政、營運、業務或其他 |
| E | 工作標題 | 任務的簡短名稱 |
| F | 工作說明 | 任務內容與預期成果 |
| G | 交付時間 | 指定的日期與時間；待確認時留白 |
| H | 備註 | LINE 紀錄、連結或補充說明 |
| I | 交付方式 | 指定時間或其他／待確認 |
| J | 狀態 | 下拉選單：待處理、進行中或已完成 |
| K | 交付回覆 | Jia 提供的完成內容或連結 |
| L | 最後更新時間 | 最近一次狀態更新時間 |

不要自行調換、插入或刪除 A–L 欄。若需要新增內部註記欄，建議從 M 欄之後開始。

---

# 二、建立綁定的 Apps Script

## 3. 從試算表開啟 Apps Script

一定要從這份試算表內開啟：

1. 點選上方「擴充功能」。
2. 選擇「Apps Script」。
3. 等待 Apps Script 編輯器開啟。
4. 專案名稱建議改為：`Jia 協作需求後端`。

這樣建立的是「綁定指令碼」，可在第一次設定時識別目前這份試算表。

## 4. 貼入完整後端程式

1. 在 Apps Script 左側選擇 `Code.gs`。
2. 刪除編輯器中原本的 `myFunction` 範例。
3. 開啟網站資料夾中的 [`google-apps-script/Code.gs`](./google-apps-script/Code.gs)。
4. 複製檔案全部內容。
5. 貼到 Apps Script 的 `Code.gs`。
6. 點選上方儲存圖示，或按 `Command + S`。

請確認函式選單中可以找到：

```text
setupSpreadsheet
doGet
doPost
```

## 5. 設定台北時區

1. 在 Apps Script 左側點選齒輪「專案設定」。
2. 找到時區。
3. 選擇 `(GMT+08:00) 台北標準時間`。
4. 返回編輯器。

時區會影響任務編號日期、建立時間與試算表顯示時間。

---

# 三、執行一次試算表初始化

這一步不能省略。正式網頁應用程式不應依賴「目前開啟中的試算表」，因此程式會先記住這份試算表的 ID，之後使用該 ID 穩定讀寫。

## 6. 執行 `setupSpreadsheet`

1. 回到 Apps Script 編輯器。
2. 在上方函式下拉選單選擇 `setupSpreadsheet`。
3. 點選「執行」。
4. 第一次執行會要求授權。
5. 點選「審查權限」。
6. 選擇建立此試算表的 Google 帳號。
7. 確認專案名稱是剛建立的 `Jia 協作需求後端`。
8. 允許程式存取這份試算表。

若公司帳號顯示未驗證或封鎖畫面：

- 先確認這是你本人剛建立的 Apps Script 專案。
- 若公司 Workspace 管理員禁止未驗證應用程式，請交由資訊部門核准，不要嘗試繞過公司政策。

## 7. 確認初始化成功

執行完成後應看到：

- Apps Script 執行狀態顯示完成。
- 試算表新增名為「工作需求」的工作表。
- 第一列自動出現 A–L 欄標題。
- 第一列為深綠色標題列並被凍結。
- J 欄從第 2 列開始提供「待處理／進行中／已完成」下拉選單。

J 欄可以直接在 Google Sheets 選擇狀態；修改 J 欄狀態或 K 欄交付回覆時，L 欄最後更新時間會自動更新。追蹤頁重新查詢或按下「更新狀態」後，即會顯示最新內容。

若沒有建立工作表：

1. 在 Apps Script 左側選擇「執行項目」。
2. 開啟最新一筆失敗紀錄。
3. 查看錯誤訊息。
4. 常見原因是沒有從正確試算表開啟 Apps Script，或沒有完成 Google 授權。

---

# 四、部署網頁應用程式

## 8. 建立正式部署

1. 在 Apps Script 右上角點選「部署」。
2. 選擇「新增部署作業」。
3. 點選「選取類型」旁的齒輪。
4. 選擇「網頁應用程式」。
5. 說明填入：`Jia 協作需求後端 v1`。
6. 「執行身分」選擇：`我`。
7. 選擇允許存取的對象。
8. 點選「部署」。
9. 複製「網頁應用程式網址」。

正式網址應以 `/exec` 結尾，例如：

```text
https://script.google.com/macros/s/部署代碼/exec
```

不要將 `/dev` 測試部署網址填入正式網站。`/dev` 只供具有 Apps Script 編輯權限的人測試最新程式。

## 9. 如何選擇存取權限

### 選項 A：任何人

優點：

- GitHub Pages 最容易直接連線。
- 發任務人不需要額外登入 Apps Script。

風險：

- 擁有後端網址的人可能呼叫後端。
- 網站目前沒有公司 SSO。
- 不適合敏感資料。

### 選項 B：公司網域內使用者

優點：

- 後端可限制公司 Google Workspace 使用者。

可能問題：

- GitHub Pages 與 Apps Script 屬於不同網站，瀏覽器的登入與跨站限制可能造成 `Failed to fetch`。
- 所有實際使用者都必須登入公司 Google 帳號。
- 是否可用會受公司 Workspace 管理政策影響。

建議先使用不敏感的測試資料驗證公司網域權限。如果 GitHub Pages 無法跨站存取，再與資訊部門確認 SSO、內網託管或其他安全方案。

---

# 五、直接測試 Apps Script 後端

## 10. 測試清單 API

將 `/exec` 網址後面加上：

```text
?action=list
```

完整範例：

```text
https://script.google.com/macros/s/部署代碼/exec?action=list
```

全新資料庫預期回應：

```json
{"ok":true,"items":[]}
```

如果顯示：

```json
{"ok":false,"error":"尚未設定試算表。請先在 Apps Script 編輯器中執行 setupSpreadsheet。"}
```

請回到第三章重新執行 `setupSpreadsheet`。

## 11. 目前不需要手動測試 POST

瀏覽器網址列只能方便測試 GET。新增與更新會由網站發出 POST，因此直接使用網站完成端到端測試即可。

---

# 六、將 `/exec` 網址接到網站

## 12. 修改 `config.js`

開啟 [`config.js`](./config.js)，找到：

```javascript
API_URL: "",
```

貼入 `/exec` 網址：

```javascript
window.APP_CONFIG = Object.freeze({
  API_URL: "https://script.google.com/macros/s/你的部署代碼/exec",
  APP_NAME: "給 Jia 的協作需求",
  SHEET_NAME: "工作需求",
});
```

檢查重點：

- 網址必須放在雙引號內。
- 行末逗號必須保留。
- 使用 `/exec`，不要使用 `/dev`。
- 不要貼入 Google Sheets 的網址。

---

# 七、完成端到端測試

## 13. 測試新增任務

1. 開啟 `index.html`。
2. 確認右上方顯示「Google Sheets 已連線」。
3. 填寫一筆測試任務。
4. 交付時間分別測試「指定日期時間」與「其他／待確認」。
5. 送出後記下任務編號。
6. 回到 Google Sheets。
7. 確認「工作需求」工作表新增資料。

「Google Sheets 已連線」只代表 `config.js` 已填入網址；真正的連線結果仍以成功送出資料為準。

## 14. 測試 Jia 管理看板

1. 開啟 `admin.html`。
2. 確認看得到剛建立的任務。
3. 點選「處理／回覆」。
4. 將狀態改成「進行中」。
5. 輸入測試回覆並儲存。
6. 再改成「已完成」。
7. 確認 Google Sheets 的 J、K、L 欄同步更新。

## 15. 測試發任務人追蹤頁

1. 開啟 `status.html`。
2. 輸入剛才的任務編號。
3. 確認顯示「已完成」。
4. 清除任務編號，只輸入與送出時相同的填寫人內容。
5. 確認能列出近期任務並點選查看。
6. 確認看得到 Jia 的交付回覆。
7. 確認最後更新時間正確。

完成這三組測試後，Google Sheets 串接才算正式完成。

---

# 八、日常使用規則

## 可以在試算表做的事

- 查看與篩選資料。
- 調整欄寬及顏色。
- 新增篩選器或篩選檢視。
- 從 L 欄之後增加只供內部使用的欄位。
- 使用「檔案 → 版本記錄」查看或還原過去版本。

Google 官方版本記錄說明：[Find what's changed in a file](https://support.google.com/docs/answer/190843)

## 不建議直接做的事

- 不要更改「工作需求」工作表名稱。
- 不要調換或刪除 A–L 欄。
- 不要在 A–L 中間插入新欄。
- 不要大量覆蓋任務編號。
- 不要將交付時間改成無法辨識的文字。
- 不建議直接在試算表修改狀態與回覆，因為 K 欄「最後更新時間」不會自動同步；請優先使用管理看板。

---

# 九、後端程式更新流程

修改 `Code.gs` 後，只按儲存不會更新正式 `/exec` 版本。

## 16. 發布新版

1. 將新版 `Code.gs` 貼入 Apps Script。
2. 儲存。
3. 如程式仍連接同一份試算表，通常不必再次執行 `setupSpreadsheet`。
4. 點選「部署 → 管理部署作業」。
5. 選擇目前的網頁應用程式部署。
6. 點選編輯。
7. 在版本選擇中建立新版本。
8. 說明填入修改內容，例如：`v2 新增交付回覆`。
9. 點選部署。
10. 再次測試 `/exec?action=list`。

正常情況下原本的 `/exec` 網址會保持不變，`config.js` 不需要修改。

## 17. 更換試算表

若未來要改用另一份試算表：

1. 在新試算表中重新建立一份綁定 Apps Script。
2. 貼入完整 `Code.gs`。
3. 在新專案中執行 `setupSpreadsheet`。
4. 重新部署為網頁應用程式。
5. 將新的 `/exec` 網址更新到 `config.js`。

---

# 十、備份與維護

## 18. 建議備份頻率

- 測試完成後：建立一份初始備份。
- 正式上線後：每月複製一次試算表。
- 大量修改欄位或後端前：先建立測試副本。

備份命名範例：

```text
Jia 協作任務資料庫_備份_2026-08
```

## 19. 權限管理

- 試算表編輯權限只給 Jia 與必要管理者。
- 發任務的人不需要直接取得試算表權限。
- Apps Script 專案權限會跟隨綁定的試算表編輯權限。
- 定期檢查 Google Drive 共用名單。
- 若部署帳號即將停用，先由新的長期管理帳號重新建立部署並完整測試。

---

# 十一、常見錯誤排除

## `尚未設定試算表`

原因：沒有成功執行 `setupSpreadsheet`。

處理：回到 Apps Script 編輯器，選擇 `setupSpreadsheet` 並執行一次。

## `找不到目前綁定的試算表`

原因：Apps Script 不是從目標試算表的「擴充功能 → Apps Script」建立。

處理：回到目標試算表重新建立綁定指令碼。

## `Failed to fetch` 或網站送出失敗

可能原因：

- 使用了 `/dev` 網址。
- 網頁應用程式存取權限過窄。
- 公司 Google Workspace 阻擋跨站請求。
- Apps Script 尚未完成授權。
- 使用者同時登入多個 Google 帳號，導致權限判斷錯誤。

處理順序：

1. 直接開啟 `/exec?action=list`。
2. 確認使用正確公司 Google 帳號。
3. 檢查「部署 → 管理部署作業」的存取權限。
4. 使用無痕視窗登入單一公司帳號測試。
5. 查看 Apps Script「執行項目」中的錯誤紀錄。

## 網站收到舊版回應

原因：修改程式後只儲存，沒有建立新部署版本。

處理：到「部署 → 管理部署作業」編輯部署並選擇新版本。

## 試算表有資料，但管理看板沒有顯示

檢查：

- A 欄是否有任務編號。
- 欄位順序是否仍是 A–L。
- `config.js` 是否連到正確 `/exec`。
- `/exec?action=list` 是否回傳該筆資料。

## 追蹤頁找不到任務

檢查：

- 任務編號是否完整。
- 是否誤輸入空格。
- A 欄是否仍保留該任務編號。
- 填寫頁、管理頁與追蹤頁是否使用同一份 `config.js`。

---

# 十二、Google Sheets 完成標準

以下項目全部完成，才進入 GitHub Pages 上架：

- [ ] 從目標試算表建立綁定 Apps Script。
- [ ] 貼入最新版 `Code.gs`。
- [ ] 時區設為台北。
- [ ] 成功執行 `setupSpreadsheet`。
- [ ] 自動建立「工作需求」與 A–L 欄。
- [ ] J 欄顯示待處理、進行中、已完成下拉選單。
- [ ] 部署為以 `/exec` 結尾的網頁應用程式。
- [ ] `/exec?action=list` 回傳 `ok: true`。
- [ ] `config.js` 已填入 `/exec` 網址。
- [ ] 網站可以新增任務。
- [ ] 管理看板可以更新狀態與交付回覆。
- [ ] 追蹤頁可以依任務編號查詢，選填填寫人時也能正確核對。
- [ ] 已完成任務不再計入待辦工作量。
- [ ] 已建立第一份試算表備份。
